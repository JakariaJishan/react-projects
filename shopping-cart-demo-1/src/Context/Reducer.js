export const cartReducer =(state, action) => {
    switch (action.type) {
        case 'ADD_TO_CART':
            
            return {}
        case 'REMOVE_FROM_CART':
            
            return {}
    
        default:
            break;
    }
}